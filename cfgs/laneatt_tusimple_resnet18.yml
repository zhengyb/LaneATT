# Model settings
val_every: 10
model_checkpoint_interval: 1
seed: 0
model:
  name: LaneATT
  parameters:
    backbone: resnet18
    S: &S 72
    topk_anchors: 1000
    #anchors_freq_path: 'data/tusimple_anchors_freq.pt'
    #anchors_freq_path: 'data/tusimple_250418pm_anchors_mask.pt'
    anchors_freq_path: 'data/tusimple_carla_anchor_mask.pt'
    #anchors_freq_path: 'data/sum_anchors_mask.pt'
    img_h: &img_h 360
    img_w: &img_w 640
batch_size: 8
epochs: 100
loss_parameters: {}
train_parameters:
  conf_threshold:
  nms_thres: 15.
  nms_topk: 3000
test_parameters:
  conf_threshold: 0.3 #0.2
  nms_thres: 50. #45.
  nms_topk: &max_lanes 4
optimizer:
  name: Adam
  parameters:
    lr: 0.0001
lr_scheduler:
  name: CosineAnnealingLR
  parameters:
    T_max: 164745 # 100 * 454 iterations

# Dataset settings
datasets:
  train: &train
    type: LaneDataset
    parameters:
      S: *S
      dataset: tusimple
      split: train
      img_size: [*img_h, *img_w]
      max_lanes: *max_lanes
      normalize: false
      aug_chance: 1.0
      augmentations:
        - name: Affine
          parameters:
            translate_px:
              x: !!python/tuple [-25, 25]
              y: !!python/tuple [-10, 10]
            rotate: !!python/tuple [-6, 6]
            scale: !!python/tuple [0.85, 1.15]
        - name: HorizontalFlip
          parameters:
            p: 0.5
      root: "datasets/" 
      #root: "datasets/TUSimple/tusimple"

  test:
    type: LaneDataset
    parameters:
      S: *S
      dataset: tusimple
      split: test
      img_size: [*img_h, *img_w]
      max_lanes: *max_lanes
      normalize: false
      aug_chance: 0
      augmentations:
      root: "datasets/"       #root: "datasets/TUSimple/tusimple-test"

  val:
    type: LaneDataset
    parameters:
      S: *S
      dataset: tusimple
      split: val
      img_size: [*img_h, *img_w]
      max_lanes: *max_lanes
      normalize: false
      aug_chance: 0
      augmentations:
      root: "datasets/" 
      #root: "datasets/TUSimple/tusimple"
